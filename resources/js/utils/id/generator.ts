export function createNewMaterialInputId(topicId: number) {
    return `new-material-input-${topicId}`
}

export function createNewMaterialTitleInputId(topicId: number) {
    return `new-material-title-input-${topicId}`
}

export function createNewMaterialSectionId(topicId: number) {
    return `new-material-section-${topicId}`
}